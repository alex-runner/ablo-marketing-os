.refresh.log
.launchd.out.log
.launchd.err.log
.DS_Store
__pycache__/
.claude-flow/
.claude/
gen_marketing_call.py
.playwright-mcp/
try-*.png
# Local product/test screenshots dropped at the repo root (not part of the OS).
# docs/*.png (e.g. the architecture render) stays tracked. Use `git add -f` to force a root png.
/*.png
